
<h1>{{title}}</h1>

<form method="post" class="feedback">
    <input type="hidden" name="lang" value="{{lang}}">
    <label>
        {{#if (eq lang "es")}}
            N
        {{else}}
            N
        {{/if}}
    </label>
</form>