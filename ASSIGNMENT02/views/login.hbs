{{!-- Refer to week 7 of professors github --}}

<h1>
    {{#if (eq lang "es")}}
        Iniciar sesión
    {{else}}
       Login
    {{/if}}
</h1>

{{!-- error message for invalid credentiasl --}}
{{if messages}}
    <div class="alert alert-danger">
        Invalid Credentials!!!
    </div>
{{else}}
    <div class="alert alert-info">
        Please enter your credentials.
    </div>
{{/if}}
<form method="post">
    {{!-- user and pass --}}
    <fieldset></fieldset>
    {{!-- if no account, direct to register --}}
    <fieldset></fieldset>
</form>